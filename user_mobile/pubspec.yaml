name: sesuatu_darikota_malang
description: "Sesuatu DariKota Malang — customer & creator storefront (Flutter, frontend only)."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  go_router: ^14.2.0
  google_fonts: ^6.2.1
  qr_flutter: ^4.1.0
  url_launcher: ^6.3.0
  intl: ^0.19.0
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
